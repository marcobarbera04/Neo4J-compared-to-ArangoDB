services:
  arangodb:
    image: arangodb
    container_name: arangodb-instance
    ports:
      - "8529:8529"
    environment:
      - ARANGO_ROOT_PASSWORD=secret
    volumes:
      - arangodb_data:/data
      - ./import:/import
    restart: unless-stopped

volumes:
  arangodb_data:
